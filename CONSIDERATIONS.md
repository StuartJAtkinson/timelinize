# Considerations — timelinize

- **The manual-entry-item concept has 3-4 different names across the app.** `input.html`'s on-page heading says "Data input"; the nav menu calls it "Manual input" (`index.html:286`); the dead `new.html` calls it "New Item"/"Manual Entry". Beyond the narrow `<title>` typo fixed in ISSUES.md, there's a broader question of which single term should be used everywhere (nav label, page heading, and — once `new.html` is either deleted or finished — its own copy). Needs a call on the canonical term.
- **"Merge" action colour disagrees between the Entities list and an Entity's own detail page.** `entities.html:61` (list bulk-merge) uses `btn btn-ghost-warning`; `entity.html:35` (single-entity page) uses `btn btn-primary`; the merge confirm modal (`entities.html:137`) uses `btn-warning`. Same underlying action rendered as warning in two places and primary in one — needs a decision on whether "merge" is a neutral/primary action or a cautionary one, then align all three.
- **PR #198 ("Dockerfile: pin libvips to v8.18.4 to fix broken build") cannot be merged by `StuartJAtkinson`.** `gh pr merge 198 --merge --delete-branch` rejected with GraphQL error: `StuartJAtkinson does not have the correct permissions to execute MergePullRequest (mergePullRequest)`. All checks passing (only `license/cla` = pass). Repo owner/maintainer needs to merge or grant merge rights to this account.
- **PR #197 ("whatsapp: support 2-digit-year date format and locale-less placeholders") cannot merge: `license/cla` check pending.** `gh pr checks 197` reports the only check (`license/cla`) as `pending` — "Contributor License Agreement is not signed yet." The CLA assistant hasn't been signed for this PR's author; until the contributor signs the CLA, this check will not pass and the merge remains blocked.
- **PR #186 ("Add new LINE Chat data source") cannot be merged by `StuartJAtkinson`.** `gh pr merge 186 --merge --delete-branch` rejected with GraphQL error: `StuartJAtkinson does not have the correct permissions to execute MergePullRequest (mergePullRequest)`. All checks passing (only `license/cla` = pass); PR is MERGEABLE. Repo owner/maintainer needs to merge or grant merge rights to this account.
- **PR #184 ("Feat goodreads import clean") cannot be merged by `StuartJAtkinson`.** `gh pr merge 184 --merge --delete-branch` rejected with GraphQL error: `StuartJAtkinson does not have the correct permissions to execute MergePullRequest (mergePullRequest)`. All checks passing (`license/cla`, `lint`, `test`); PR is MERGEABLE. Repo owner/maintainer needs to merge or grant merge rights to this account.
- **PR #163 ("feat: add element/matrix datasource") cannot be merged by `StuartJAtkinson`.** `gh pr merge 163 --merge --delete-branch` rejected with GraphQL error: `StuartJAtkinson does not have the correct permissions to execute MergePullRequest (mergePullRequest)`. Only check configured is `license/cla` = pass. Repo owner/maintainer needs to merge or grant merge rights to this account.
