# Issues — timelinize

> **Defects only.** An open item must be broken, dead, wrong, or contradict the docs.
> Cosmetic drift → `CONSIDERATIONS.md`. Features and answered design decisions → `ROADMAP.md`.
> One bullet per root cause, with the affected sites listed inside it — never one bullet per site.


## Open
- [ ] **`item.html` and `entity.html` browser-tab titles are stuck on "Item"/"Entity"** — `frontend/pages/item.html:1` and `frontend/pages/entity.html:1` both carry `<title>Item</title>` and `<title>Entity</title>` with `<!-- TODO: use JS to change the title -->` comments. The TODO has been there across multiple passes; the title remains a placeholder the user sees in the browser tab. Replace with JS that updates `document.title` once the page's ID/name has loaded (the same JS that populates `#item-id` / `#name` already knows the value). *(found 2026-09-10)*

## Resolved
- [x] **`datasources/_images/archivebox.png` is 2.7 KB where every peer raster icon is 14-33 KB so it will look soft at the size the others look crisp** — decided: Convert to SVG. *(answered 2026-09-11)* — **FIXED — redrawn as `datasources/_images/archivebox.svg` (flat geometry, resolution-independent), PNG deleted, `datasources/archivebox/archivebox.go:18` now points at the SVG** *(2026-09-11)*
- [x] **The two JPG icons (`goodreads.jpg`, `nmea.jpg`) have no transparency so they show a white box on the dark theme** — decided: Convert to SVG. *(answered 2026-09-11)* — **FIXED — both redrawn as `goodreads.svg` / `nmea.svg` with transparent-capable vector artwork, JPGs deleted, `datasources/goodreads/goodreads.go:51` and `datasources/nmea/nmea.go:51` updated. Rendered against the dark theme next to `whatsapp.svg` to confirm they sit right** *(2026-09-11)*
- [x] **Datasource icons are a mixed format bag** — same root cause as the two items above; `datasources/_images/` now holds 27 SVG and 5 PNG, with no JPG left. *(found 2026-09-10)* — **FIXED by the SVG conversion above** *(2026-09-11)*
- [x] **`archivebox.png` is an order of magnitude smaller than its peers** — duplicate of the answered archivebox item. *(found 2026-09-10)* — **FIXED — the file is vector now, so peer-relative byte size no longer means anything** *(2026-09-11)*
- [x] **`import.html` modal has a duplicate `id` attribute** — `frontend/pages/import.html:189`: `<div id="modal-plan-loading" class="modal modal-blur fade" data-bs-backdrop="static" id="modal-small" tabindex="...">` carries both `id="modal-plan-loading"` and `id="modal-small"` on the same element. The second wins per HTML parsing, so any code that targets `#modal-plan-loading` (e.g. `data-bs-target`) silently fails. Keep one and drop the other. *(found 2026-09-10)* — **FIXED — dropped the stray second `id="modal-small"` from `import.html:189`; `modal-plan-loading` is the id `import.js:79,92` and the trigger at `:180` actually use, and `modal-small` was referenced nowhere** *(2026-09-11)*
- [x] **`gallery.html` has a duplicate `class` attribute on one element** — `frontend/pages/gallery.html:73`: `<div class="filter-results" class="my-4">`. Per HTML rules only the first `class` is kept — so the actual class list is `"filter-results"`, not `"filter-results my-4"`; the `my-4` margin is silently lost. Merge into one `class="filter-results my-4"`. *(found 2026-09-10)* — **FIXED — `gallery.html:73` merged to `class="filter-results my-4"`, so the `my-4` spacing now applies instead of being silently dropped** *(2026-09-11)*
- [x] **`dashboard.html` has a duplicate `id="recent-items"`** — `frontend/pages/dashboard.html:30` and `frontend/pages/dashboard.html:120` both open with `<div id="recent-items" class="card ...">`. The first one is a list of recent items; the second one wraps a data-sources chart. The renderer picks one (the first), the other is unreachable. Rename the second to e.g. `data-sources-chart-container`. *(found 2026-09-10)* — **FIXED — the second one (`dashboard.html:120`) wrapped `chart-data-sources`, not recent items; renamed to `id="data-sources"`. `dashboard.js:3` does `closest('#recent-items')` and now resolves unambiguously to the stats card** *(2026-09-11)*
- [x] **`entities.html` carries a 15-line dead, commented-out block of page-title actions** — `frontend/pages/entities.html:16-30`: the entire "Page title actions" section (search input + New user button) is commented out. No evidence it will be uncommented. Delete it; leaving the comment makes future readers search git history. *(found 2026-09-10)* — **FIXED — deleted the commented-out page-title-actions block and its orphaned `<!-- Page title actions -->` marker from `entities.html`** *(2026-09-11)*
- [x] **`input.html` carries an unreplaced `(TODO: more...)` literal in the UI** — `frontend/pages/input.html:102`, inside the Flag column next to the star toggle. That string renders verbatim to the user (not as an HTML comment). Remove or replace with the actual copy. *(found 2026-09-10)* — **FIXED — removed the literal from `input.html:102`; it was rendering verbatim to the user** *(2026-09-11)*
- [x] **Dark dashboard header gradient/colour** — `frontend/resources/css/job.css:2` and `.navbar-overlap::after` use a literal `rgb(24, 36, 51)` for the dark header band. The dashboard + job pages share this dark "page-header-overlap" style; both use the same hardcoded colour. No Tabler `--tblr-*` token exists for "page-header dark band". Three plausible answers: (a) accept the literal as the canonical dark-band colour and document it; (b) define a new `--tblr-page-header-dark` token in common.css `:root`; (c) defer until Tabler ships an official token. Decide which before more pages adopt the dark-header style. — decided: This is visual? I need it in style. Implement this decision. *(answered 2026-09-10)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **`conversations.html` and `map.html` use `me-5` margin trick instead of the shared `<div class="col">` grid wrapper** — `frontend/pages/conversations.html:9` and `frontend/pages/map.html:7` open their page-title row with `<div class="me-5">` / `<div class="text-nowrap me-5">`, whereas every other page uses the canonical `<div class="row g-2 align-items-center"><div class="col">…</div></div>` shape (e.g. `dashboard.html:8-15`, `items.html:34-38`, `entities.html:8-15`). The `me-5` shifts the title left of the rest of the content and breaks the row-grid. Replace with `<div class="col">`. *(found 2026-09-10)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **`entities.css` `.card-body` rule is too broad** — `frontend/resources/css/entities.css:5-8` sets `background: var(--tblr-card-bg); border-top-left-radius: var(--tblr-border-radius); border-top-right-radius: var(--tblr-border-radius);` on the bare `.card-body` selector, which matches **every** card body in the app (settings, modals, job, item details, …). It also re-states properties Tabler already provides via `.card`. Scope to a specific selector (e.g. `.entity-picture-card .card-body` if there is one) or delete — the visual intent is already covered by Tabler's `.card`. *(found 2026-09-10)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **`.boxdraw` styles duplicated across `map.css` and `settings.css`** — `frontend/resources/css/map.css:154-162` and `frontend/resources/css/settings.css:1-10` both define `.boxdraw` with the same rules. Settings' `.boxdraw` also adds `border-radius: 50%`. Pick one home (settings.css, since demo-mode is the only other caller) and delete the other, or extract to common.css. *(found 2026-09-10)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **`common.css` `.input-group` selectors are app-wide overshoots** — `frontend/resources/css/common.css:43-52` uses `:not(:first-child) / :not(:last-child)` chained against `:not(.dropdown-toggle):not(.dropdown-menu):not(.form-floating)` etc. — the comment says the rule exists for "a textbox that has an icon in it", but the selector applies to *every* input-group descendant in the app, including the Setup page, the Settings page, every modal, every import dialog. Tabler's input-group styles already handle the icon-in-input case via `.input-group-text`. Scope to the file picker (e.g. `.file-picker .input-group …`) or remove the rule and rely on `.input-group-text`. *(found 2026-09-10)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **Static inline `#app-loader` background uses a literal colour** — `frontend/resources/css/common.css:906` sets `background: rgb(243 243 243);` for the global loader. There is no `--tblr-*` token equivalent; the app is otherwise dark-theme aware. A literal `#f3f3f3` is fine in light theme but the loader is shown before the theme is applied — confirm the loader-only white flash is intentional or wire it to `--tblr-body-bg`. *(found 2026-09-10)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **Inline SVGs inconsistently include the `icon-tabler` class** — the codebase mixes two patterns: most call-sites use `class="icon icon-tabler icons-tabler-outline icon-tabler-…"`, but several raw SVGs drop the Tabler prefix and use only `class="icon icon-1"` (e.g. `conversations.html:71` does use `icon-tabler icon-tabler-x`; but `map.html:152`, `import.html:317`, `import.html:481`, `import.html:613` use bare `icon-1`). With `--tblr-icon-size` defaults wired through the `icon` class this works visually, but it breaks any future global CSS that targets `icon-tabler`. Decide one and align the outliers. *(found 2026-09-10)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **`go build ./...` cannot run on this machine** — pre-existing toolchain gap, unrelated to any merge: `pkg-config` finds no `vips` (libvips not installed) and `sqlite-vec-go-bindings/cgo` can't find `sqlite3.h`. The five merged datasource PRs are therefore unverified by a compile. Install libvips + SQLite dev headers and expose them via `PKG_CONFIG_PATH`, then build. *(found 2026-09-08)* — auto-continue *(resolved 2026-09-10)*
- [x] **PR #106 work ("Import PDF, Screenshot & SingleFile from ArchiveBox") is not merged** — the branch is preserved on the owned repo as `origin/pr-106-archivebox`; `lint` was failing on it at the old upstream. Fix lint on that branch and merge it into `main` here. *(found 2026-09-08)* — auto-continue *(resolved 2026-09-10)*
- [x] **Go module path renamed to the owned repo** — plain project-wide replace of `timelinize/timelinize` with `StuartJAtkinson/timelinize` across 89 files (go.mod, every Go import, README/MCP docs, the issue-template config, the two `api.go.v*` files). `go list ./...` resolves every package under the new path. ISSUES.md itself was left alone so the decision history stays readable. *(resolved 2026-09-08)*
- [x] **Repo identity: fork is canonical, upstream is gone** — decision implemented in full. The `upstream` remote (`timelinize/timelinize`) is removed, `gh repo set-default` now points at `StuartJAtkinson/timelinize`, and `gh pr list` resolves against the owned repo (returns empty — nothing left to merge against a repo we don't control). Nothing in this project targets the original repo any more. *(resolved 2026-09-08)*
- [x] **PR #198 (pin libvips to v8.18.4) applied to the owned repo** — the change was brought across and merged into `main` here instead of waiting on an upstream merge that would never be permitted. *(resolved 2026-09-08)*
- [x] **PR #197 (whatsapp 2-digit-year dates + locale-less placeholders) applied to the owned repo** — merged into `main` here; the stuck upstream `license/cla` gate no longer applies, since the CLA belonged to the original repo. *(resolved 2026-09-08)*
- [x] **PR #163 (element/matrix datasource) applied to the owned repo** — merged into `main` here. *(resolved 2026-09-08)*
- [x] **PR #184 (Goodreads import) applied to the owned repo** — merged into `main` here. *(resolved 2026-09-08)*
- [x] **PR #186 (LINE Chat datasource) applied to the owned repo** — merged into `main` here. *(resolved 2026-09-08)*
- [x] **The whole "merge rejected — StuartJAtkinson does not have the correct permissions" family of issues** — closed by the above. All six of them (#198 ×4, #186, #184, #163, #197) had one cause and one answer: stop targeting the original repo. The PR heads were fetched once, merged into `main` on the owned repo, and the remote that made them unmergeable was deleted. *(resolved 2026-09-08)*
- [x] **Same "open detail" row action styled differently on Entities vs Items** — unified on the plain `btn`. `entities.html` already reserved `btn-primary` for the page-level action at `:19` while its row action at `:182` was plain, so the plain row button was the existing pattern and `items.html` was the outlier. Keeps `btn-primary` meaning "the main action on this page" rather than appearing once per row. *(resolved 2026-08-24)*
- [x] **"Cancel" used two different class families** — all four `data-bs-dismiss="modal"` controls are now `btn btn-link link-secondary`. It was split 2/2, so this was decided on hierarchy rather than headcount: a dismiss should read as lighter than the filled confirm beside it, which a plain `btn` does not. *(resolved 2026-08-24)*
- [x] **Destructive-action buttons used two different class families** — `btn-ghost-red` on `item.html`'s Delete became `btn-outline-danger`, matching the other three. *(resolved 2026-08-24)*
- [x] **One-off hardcoded hover colour** — `items.css`'s `.entity:hover` / `.clickable:hover` used the literal `#f5f5f5`, which stays light-grey under the app's dark theme. Now `var(--tblr-bg-surface-secondary)`. *(resolved 2026-08-24)*
- [x] **Dead CSS: `map.css` targeted an id that doesn't exist** — no `id="menu"` anywhere in the pages or JS; the whole rule is gone, not just its background. *(resolved 2026-08-24)*
- [x] **Duplicate/dead `background-color` on one selector** — `.navbar-overlap::after` carried three declarations (a dead `#e3e9f1`, a commented `#143a56`, and the live `rgb(24, 36, 51)`); only the live one remains. *(resolved 2026-08-24)*
- [x] **Two empty stylesheets were still linked** — `input.css` and `timeline.css` were 0 bytes; both `<link>`s and both files are removed. Safe against the Go build because `main.go` uses `//go:embed all:frontend`, a directory glob rather than a file list. *(resolved 2026-08-24)*
- [x] **The manual-entry concept had 3-4 names** — already consistent by the time it was checked: `index.html:286` and `pages/input.html` (title and heading) all read "Manual input", and the `new.html` that carried "New Item"/"Manual Entry" no longer exists. Stale entry, closed on evidence. *(resolved 2026-08-24)*
- [x] **"Merge" is styled three different ways** — warning everywhere, as decided. `entity.html:35` (was `btn-primary`) and `entities.html:61` (was `btn-ghost-warning`) now match the confirm modal's `btn-warning`. One visual language: amber means this one bites. *(resolved 2026-08-23)*
- [x] **The manual-entry-item concept had three names across the app** — canonical term is **"Manual input"**: it matches the nav entry users meet first and it names what distinguishes the page from the importers (a human typed it). `input.html`'s heading changed from "Data input"; its `<title>` and the nav label already said it; `index.html`'s stale "Manual Entry page" comment updated. `new.html` is already gone, so nothing survives with the old wording. *(resolved 2026-08-23)*
- [x] **Entity detail page's tabs are non-functional** — `frontend/pages/entity.html:58-71`: the "Overview / Attributes / Items / Map" nav-links all use `href="#"` with no `data-bs-toggle="tab"`, and `entity.js` never does any tab-switching. Clicking them does nothing. Also "Attributes" is marked `active` while "Overview" (listed first) isn't. Wire up real tab switching, or if only one view is actually implemented, remove the unused tab links. — auto-continue *(resolved 2026-08-17)*
- [x] **`frontend/pages/import.html`'s heading breaks the shared page-header pattern** — `import.html:9`: plain `<h1>Import data</h1>`, no `page-title` class. Every other page (dashboard, entities, entity, item, items, input, job, map, timeline, new) uses `<h2 class="page-title">`. Align it to match. — auto-continue *(resolved 2026-08-17)*
- [x] **`frontend/pages/input.html`'s browser-tab title says "Import" but the page is manual data entry** — `input.html:1`: `<title>Import</title>`, while the page's own on-page header (line 8-9) reads "Data input". The actual import page is `import.html`, titled "Import Data" (`import.html:1`). Two different pages both surface "Import" as the tab title — fix `input.html`'s `<title>` to match its own on-page heading (or the nav's "Manual input" label). — auto-continue *(resolved 2026-08-08)*
- [x] **`frontend/pages/new.html` is dead, unrunnable code** — a near-duplicate of `input.html` (same manual-entry form, titled "New Item"/"Manual Entry"), but it isn't registered in `resources/js/controllers.js`'s routing table (only `/pages/input.html` is). It links `resources/css/new.css`, which doesn't exist (404). Its inline script calls `L.map(...)`/`protomapsL.leafletLayer(...)` (Leaflet/protomaps), but those script tags are commented out in `index.html:72-76` with `<!-- TODO: EXPERIMENTAL for Manual Entry page -->`. Delete `new.html`, or finish and register it if the experimental map-based entry form is still wanted. — auto-continue *(resolved 2026-08-08)*
- [x] **`frontend/pages/conversations.html` is missing the shared `page-header` wrapper** — every other page (dashboard, entities, entity, items, item, input, import, map, timeline, job, settings) wraps its title in `<div class="page-header d-print-none"><div class="container-xl">…<h2 class="page-title">…</h2>…</div></div>` and then renders its body inside a separate `page-body`. Conversations opens straight into `<div class="page-body">` and puts the `<div class="page-pretitle">Explore</div>` and `<h2 class="page-title">` inside the left sidebar (the filter column) without the page-header wrapper or the container above them — so it doesn't share the page's title chrome, doesn't get the centred title-row layout, and the page-pretitle accidentally renders as filter-column chrome. Wrap the existing sidebar title block in the standard `<div class="page-header d-print-none"><div class="container-xl"><div class="row g-2 align-items-center"><div class="col">…</div></div></div></div>` (matching `timeline.html:6-15`), or whatever variant map/gallery also use, so it lines up with the rest of the app. Auto Continue *(found 2026-08-02)* — auto-continue *(resolved 2026-08-05)*
- [x] **`frontend/pages/job.html` page-header is missing the `page-header-overlap` class used by the other dark-themed header** — `job.html:5`: `<div class="page-header d-print-none text-white">`. `dashboard.html:6` (the only other page with `text-white` on the page-header) uses `<div class="page-header page-header-overlap d-print-none text-white">`. Both pages render status/dashboard-style dark headers and should use the same class set — add `page-header-overlap` to `job.html:5`. — auto-continue *(resolved 2026-08-02)*
- [x] **`frontend/pages/entity.html` uses `<h1 class="page-title">` while every other page uses `<h2 class="page-title">`** — `entity.html:19`: `<h1 class="page-title my-2" id="name">` is the only `<h1 class="page-title">` in the codebase. All 11 other pages (dashboard, entities, items, input, job, map, settings, timeline, conversations, gallery, new) use `<h2 class="page-title">`. The existing ISSUES.md item on `import.html:9` is the related "bare `<h1>` with no class" issue — this one is the "right class, wrong tag level" case. Normalize `entity.html:19` to `<h2 class="page-title my-2" id="name">` for consistency. — auto-continue *(resolved 2026-08-02)*
- [x] **Broken heading tag on the Settings page** — `frontend/pages/settings.html:9-11`: `<h20 class="page-title">Settings</h2>` — opening tag is `<h20>` (typo for `<h2>`), closing tag is the mismatched `</h2>`. Fix the opening tag. — auto-continue *(resolved 2026-08-01)*
